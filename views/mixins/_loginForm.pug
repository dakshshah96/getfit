mixin loginForm()
    p Not registered yet? 
        a(href=`/register`) Sign Up →
    p Make an account or use the test account
    p <strong>Email</strong>: test@example.com | <strong>Password</strong>: test
    form(action="/login" method="POST" class="card")
        h2 Login
        .form-contents
            label(for="email") Email Address
            input(type="email" placeholder="someone@example.com" name="email" required) 
            label(for="password") Password
            input(type="password" name="password" required)
            input.button(type="submit" value="Log In →")